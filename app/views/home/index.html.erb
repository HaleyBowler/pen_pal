<!--#page with 3 people to message -->
<div class="jumbotron jumbotron-home">
  <div class="container home">
    <div class="page-header">
    <h17>Welcome to Chatable <br \>
    <!-- Button trigger modal -->
<button type="button" class="btn btn-lg btn-primary blue-btn" data-toggle="modal" data-target="#myModal">
  Update Languages
</button>
    </div>
  </div>
</div>
<br \>

<div class="container user_table">
  <div class="row">
		<% @users.each do |user| %>
      <div class="col-sm-4">
			    <li class="random-users"><%= render partial: "display", locals: {user: user, languages: user.languages} %></li><br \>
          </div>
		<% end %>
  </div>
</div>

<%= render partial: 'modal', locals: {user: @user} %>